<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_name %><br>
    <%= f.text_field :item_name %>
  </div>
  <div class="field">
    <%= f.label :item_price %><br>
    <%= f.number_field :item_price %>
  </div>
  <div class="field">
    <%= f.label :item_quanity %><br>
    <%= f.number_field :item_quanity %>
  </div>
  <div class="field">
    <%= f.label :item_category %><br>
    <%= f.text_field :item_category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
